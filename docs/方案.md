我来帮你实现项目初始化（Day 1-2）的所有步骤。让我们按照 TODO LIST 的形式逐步完成：

## 📋 Day 1-2 项目初始化 - TODO LIST

### ✅ 第一步：创建后端目录结构

```bash
cd d:\Code\Python\cell

# 创建后端目录
mkdir backend

# 进入后端目录
cd backend

# 创建应用子目录
mkdir app

# 进入 app 目录
cd app

# 创建功能模块目录
mkdir api models services utils temp logs configs weights exports

# 返回根目录
cd ../../
```

**验证结果：**
```bash
# 查看目录结构
tree /F backend
```

---

### ✅ 第二步：创建 .gitignore 文件

```bash
cd d:\Code\Python\cell

# 使用记事本创建 .gitignore
notepad .gitignore
```

**复制以下内容到 .gitignore：**
```gitignore
# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
venv/
ENV/
build/
*.egg-info/
.installed.cfg
*.egg

# Node
node_modules/
npm-debug.log*
dist/

# IDE
.vscode/
.idea/
*.swp
*~

# 项目特定
backend/temp/*
!backend/temp/.gitkeep
backend/logs/*
!backend/logs/.gitkeep
backend/exports/*
!backend/exports/.gitkeep
backend/weights/*.pth
*.env
.env.local
```

**保存并关闭记事本**

---

### ✅ 第三步：创建 .gitkeep 占位文件

```bash
cd d:\Code\Python\cell

# 创建空的 .gitkeep 文件（PowerShell）
New-Item -ItemType File -Path "backend\temp\.gitkeep" -Force
New-Item -ItemType File -Path "backend\logs\.gitkeep" -Force
New-Item -ItemType File -Path "backend\exports\.gitkeep" -Force
New-Item -ItemType File -Path "backend\configs\.gitkeep" -Force
New-Item -ItemType File -Path "backend\weights\.gitkeep" -Force
```

**验证结果：**
```bash
# 检查文件是否创建成功
ls backend\temp\.gitkeep
ls backend\logs\.gitkeep
ls backend\exports\.gitkeep
```

---

### ✅ 第四步：初始化 Git 仓库

```bash
cd d:\Code\Python\cell

# 初始化 Git 仓库
git init

# 查看所有文件状态
git status

# 添加所有文件到暂存区
git add .

# 第一次提交
git commit -m "Initial project structure - 项目初始化"

# 查看提交历史
git log --oneline
```

**预期输出：**
```
Initialized empty Git repository in d:/Code/Cell/.git/
[master (root-commit) abc1234] Initial project structure - 项目初始化
 X files changed, X insertions(+)
```

---

### ✅ 第五步：创建 Python 虚拟环境

```bash
cd d:\Code\Python\cell\backend

# 创建虚拟环境
python -m venv venv

# 验证虚拟环境已创建
ls venv\Scripts\activate.bat
```

**预期结果：** 看到 `venv` 文件夹

---

### ✅ 第六步：激活虚拟环境

```bash
cd d:\Code\Python\cell\backend

# PowerShell 激活虚拟环境
.\venv\Scripts\Activate.ps1
```

**如果提示权限错误，执行：**
```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

**然后再次激活：**
```powershell
.\venv\Scripts\Activate.ps1
```

**验证激活成功：** 命令行前会出现 `(venv)` 前缀

---

### ✅ 第七步：创建 requirements.txt 文件

```bash
cd d:\Code\Python\cell\backend

# 创建 requirements.txt
notepad requirements.txt
```

**复制以下内容：**
```txt
# Web 框架
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6

# 数据处理
pydantic==2.5.0
pydantic-settings==2.1.0
numpy==1.26.2

# 图像处理
pillow==10.1.0

# 工具库
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# 测试
pytest==7.4.3
httpx==0.25.2
```

**保存并关闭记事本**

---

### ✅ 第八步：安装 Python 依赖

```bash
cd d:\Code\Python\cell\backend

# 确保虚拟环境已激活
.\venv\Scripts\Activate.ps1

# 安装依赖（使用国内镜像加速）
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple

# 验证安装
pip list
```

**预期输出（部分）：**
```
Collecting fastapi==0.104.1
  Downloading...
Successfully installed fastapi-0.104.1 uvicorn-0.24.0 ...
```

---

### ✅ 第九步：创建 FastAPI 主应用

```bash
cd d:\Code\Python\cell\backend\app

# 创建 main.py 文件
notepad main.py
```

**复制以下代码到 main.py：**
```python
from fastapi import FastAPI
from fastapi.middleware.cors import CORSMiddleware
import uvicorn
from pathlib import Path

# 创建 FastAPI 应用
app = FastAPI(
    title="细胞智能计数系统",
    version="1.0.0",
    description="细胞智能计数系统 API"
)

# 配置 CORS
app.add_middleware(
    CORSMiddleware,
    allow_origins=["http://localhost:3000", "http://localhost:5173"],
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)

# 健康检查接口
@app.get("/health")
async def health_check():
    return {
        "status": "ok",
        "version": "1.0.0"
    }

# 根路径
@app.get("/")
async def root():
    return {
        "message": "欢迎使用细胞智能计数系统 API",
        "docs": "/docs"
    }

if __name__ == "__main__":
    uvicorn.run("app.main:app", host="0.0.0.0", port=8000, reload=True)
```

**保存并关闭记事本**

---

### ✅ 第十步：创建配置文件

```bash
cd d:\Code\Python\cell\backend\app

# 创建 config.py 文件
notepad config.py
```

**复制以下代码到 config.py：**
```python
from pydantic_settings import BaseSettings
from pathlib import Path
import os

class Settings(BaseSettings):
    # 基础配置
    PROJECT_NAME: str = "细胞智能计数系统"
    VERSION: str = "1.0.0"
    
    # 服务器配置
    HOST: str = "0.0.0.0"
    PORT: int = 8000
    
    # 文件配置
    BASE_DIR: Path = Path(__file__).parent.parent
    UPLOAD_DIR: Path = BASE_DIR / "temp" / "uploads"
    EXPORT_DIR: Path = BASE_DIR / "temp" / "exports"
    MAX_FILE_SIZE: int = 20 * 1024 * 1024  # 20MB
    
    class Config:
        env_file = ".env"
        case_sensitive = True

settings = Settings()

# 确保目录存在
os.makedirs(settings.UPLOAD_DIR, exist_ok=True)
os.makedirs(settings.EXPORT_DIR, exist_ok=True)
```

**保存并关闭记事本**

---

### ✅ 第十一步：创建环境变量文件

```bash
cd d:\Code\Python\cell\backend

# 创建 .env 文件
notepad .env
```

**复制以下内容到 .env：**
```bash
PROJECT_NAME=细胞智能计数系统
VERSION=1.0.0
PORT=8000
```

**保存并关闭记事本**

---

### ✅ 第十二步：创建前端项目

```bash
cd d:\Code\Python\cell

# 使用 Vite 创建 React + TypeScript 项目
npm create vite@latest frontend -- --template react-ts

# 进入前端目录
cd frontend

# 安装基础依赖
npm install

# 安装额外依赖
npm install antd axios react-router-dom

# 安装类型定义
npm install -D @types/node
```

**预期输出：**
```
✓ Project created successfully
added X packages
```

---

### ✅ 第十三步：配置 Vite 代理

```bash
cd d:\Code\Python\cell\frontend

# 编辑 vite.config.ts
notepad vite.config.ts
```

**将内容替换为：**
```typescript
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'
import path from 'path'

export default defineConfig({
  plugins: [react()],
  resolve: {
    alias: {
      '@': path.resolve(__dirname, './src'),
    },
  },
  server: {
    port: 5173,
    proxy: {
      '/api': {
        target: 'http://localhost:8000',
        changeOrigin: true,
      },
    }
  }
})
```

**保存并关闭记事本**

---

### ✅ 第十四步：更新前端 App.tsx

```bash
cd d:\Code\Python\cell\frontend\src

# 编辑 App.tsx
notepad App.tsx
```

**将内容替换为：**
```tsx
import { BrowserRouter, Routes, Route } from 'react-router-dom'
import { ConfigProvider } from 'antd'
import zhCN from 'antd/locale/zh_CN'
import HomePage from '@/pages/HomePage'
import UploadPage from '@/pages/UploadPage'

function App() {
  return (
    <ConfigProvider locale={zhCN}>
      <BrowserRouter>
        <Routes>
          <Route path="/" element={<HomePage />} />
          <Route path="/upload" element={<UploadPage />} />
        </Routes>
      </BrowserRouter>
    </ConfigProvider>
  )
}

export default App
```

**保存并关闭记事本**

---

### ✅ 第十五步：创建首页组件

```bash
cd d:\Code\Python\cell\frontend\src

# 创建 pages 目录
mkdir pages

# 创建 HomePage.tsx
notepad pages\HomePage.tsx
```

**复制以下代码：**
```tsx
import { Button, Typography } from 'antd'
import { useNavigate } from 'react-router-dom'

const { Title } = Typography

const HomePage = () => {
  const navigate = useNavigate()

  return (
    <div style={{ padding: 40, textAlign: 'center' }}>
      <Title>细胞智能计数系统</Title>
      <Button 
        type="primary" 
        size="large"
        onClick={() => navigate('/upload')}
      >
        开始上传图像
      </Button>
    </div>
  )
}

export default HomePage
```

**保存并关闭记事本**

---

### ✅ 第十六步：创建上传页面组件

```bash
cd d:\Code\Python\cell\frontend\src\pages

# 创建 UploadPage.tsx
notepad UploadPage.tsx
```

**复制以下代码：**
```tsx
import { Upload, message } from 'antd'
import { InboxOutlined } from '@ant-design/icons'

const { Dragger } = Upload

const UploadPage = () => {
  const uploadProps = {
    name: 'file',
    action: '/api/upload/single',
    multiple: false,
    maxCount: 1,
    accept: 'image/jpeg,image/png',
    beforeUpload: (file: File) => {
      // 验证文件大小
      if (file.size > 20 * 1024 * 1024) {
        message.error('文件大小不能超过 20MB')
        return false
      }
      return true
    },
    onChange: (info: any) => {
      if (info.file.status === 'done') {
        message.success(`${info.file.name} 上传成功`)
      } else if (info.file.status === 'error') {
        message.error(`${info.file.name} 上传失败`)
      }
    },
  }

  return (
    <div style={{ padding: 40 }}>
      <Dragger {...uploadProps}>
        <p className="ant-upload-drag-icon">
          <InboxOutlined />
        </p>
        <p className="ant-upload-text">点击或拖拽文件到此区域上传</p>
        <p className="ant-upload-hint">
          支持 JPEG/PNG 格式，单个文件不超过 20MB
        </p>
      </Dragger>
    </div>
  )
}

export default UploadPage
```

**保存并关闭记事本**

---

### ✅ 第十七步：Git 提交

```bash
cd d:\Code\Python\cell

# 查看所有变更
git status

# 添加所有新文件
git add .

# 提交
git commit -m "feat: 完成项目初始化 - 前后端基础架构搭建"
```

---

## 🎉 完成检查清单

请逐项检查以下内容：

### 后端检查
- [ ] `backend/app/main.py` 已创建
- [ ] `backend/app/config.py` 已创建
- [ ] `backend/.env` 已创建
- [ ] `backend/requirements.txt` 已创建
- [ ] 虚拟环境已激活（命令行显示 `(venv)`）
- [ ] 依赖包已安装（运行 `pip list` 能看到 fastapi、uvicorn 等）

### 前端检查
- [ ] `frontend/package.json` 已创建
- [ ] `frontend/vite.config.ts` 已创建
- [ ] `frontend/src/App.tsx` 已更新
- [ ] `frontend/src/pages/HomePage.tsx` 已创建
- [ ] `frontend/src/pages/UploadPage.tsx` 已创建
- [ ] 依赖已安装（`node_modules` 文件夹存在）

### Git 检查
- [ ] `.gitignore` 已创建
- [ ] `.gitkeep` 文件已创建
- [ ] Git 仓库已初始化
- [ ] 代码已提交到 Git

---

## 🚀 下一步测试

现在可以测试项目是否初始化成功：

**启动后端服务：**
```bash
cd d:\Code\Python\cell\backend
.\venv\Scripts\Activate.ps1
python -m uvicorn app.main:app --reload
```

**新开一个终端，启动前端服务：**
```bash
cd d:\Code\Python\cell\frontend
npm run dev
```

**访问测试：**
- 后端：http://localhost:8000/health
- 前端：http://localhost:5173
- Swagger 文档：http://localhost:8000/docs

**预期结果：**
- 后端返回 JSON：`{"status":"ok","version":"1.0.0"}`
- 前端显示首页和"开始上传图像"按钮

---

✅ **Day 1-2 项目初始化完成！** 

准备好继续下一步了吗？我可以帮你实现上传功能或者预分析模块。