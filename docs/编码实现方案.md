# 细胞智能计数系统 - 编码实现方案 v1.0

**作者：** 卢伶利  
**创建时间：** 2026-03-07  
**预计周期：** 8 周  
**难度等级：** ⭐⭐⭐⭐  

---

## ⚠️ 重要提示：Windows PowerShell 用户必读

本方案所有命令均针对 **Windows PowerShell** 编写，与 Linux Bash 命令有所不同。

**关键区别：**
- ❌ PowerShell **不支持** `mkdir dir1 dir2 dir3`（一次创建多个目录）
- ✅ 请使用 `New-Item -ItemType Directory -Path dir1,dir2,dir3 -Force`

**详细命令参考：** [PowerShell 命令参考](./PowerShell 命令参考.md)

**常见错误解决方案：**
```powershell
# 如果遇到“无法加载文件”错误，执行此命令修改执行策略
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

---

## 📋 目录

1. [开发准备阶段](#1-开发准备阶段)
2. [第一阶段：基础架构搭建](#2-第一阶段基础架构搭建-week-1-2)
3. [第二阶段：核心功能开发](#3-第二阶段核心功能开发-week-3-5)
4. [第三阶段：功能完善](#4-第三阶段功能完善-week-6-7)
5. [第四阶段：测试与部署](#5-第四阶段测试与部署-week-8)
6. [每日检查清单](#6-每日检查清单)
7. [常见问题与解决方案](#7-常见问题与解决方案)
8. [PowerShell 命令参考](./PowerShell 命令参考.md)

---

## 1. 开发准备阶段

### 1.1 环境检查与配置（Day 1）

#### ✅ 步骤 1.1.1：Python 环境安装

```bash
# 1. 安装 Python 3.9+（推荐 3.9.13）
# 下载地址：https://www.python.org/downloads/
# ⚠️ 安装时勾选 "Add Python to PATH"

# 2. 验证安装
python --version  # 应显示 Python 3.9.x
pip --version     # 应显示 pip 22.x+

# 3. 升级 pip
python -m pip install --upgrade pip
```

#### ✅ 步骤 1.1.2：Node.js 环境安装

```bash
# 1. 安装 Node.js 18.x LTS
# 下载地址：https://nodejs.org/

# 2. 验证安装
node --version    # 应显示 v18.x.x
npm --version     # 应显示 9.x+
```

#### ✅ 步骤 1.1.3：CUDA 环境配置（如有 GPU）

```bash
# 1. 检查 NVIDIA 驱动
nvidia-smi  # 查看 CUDA 版本和 GPU 信息

# 2. 安装 CUDA 11.7+（可选，CPU 模式可跳过）
# 下载地址：https://developer.nvidia.com/cuda-toolkit-archive

# 3. 安装 cuDNN
# 下载地址：https://developer.nvidia.com/cudnn
```

#### ✅ 步骤 1.1.4：Git 安装

```bash
# 下载安装 Git
# https://git-scm.com/download/win

# 配置用户信息
git config --global user.name "Luli Lili"
git config --global user.email "your.email@example.com"
```

---

### 1.2 项目初始化（Day 1-2）

#### ✅ 步骤 1.2.1：创建项目目录结构

```bash
cd d:\Code\Python\cell

# 创建后端目录结构（PowerShell 语法）
New-Item -ItemType Directory -Path backend,frontend -Force

# 进入后端目录
cd backend

# 创建后端子目录
New-Item -ItemType Directory -Path app,temp,logs,configs,exports,weights -Force

# 进入 app 目录
cd app

# 创建功能模块目录
New-Item -ItemType Directory -Path api,models,services,utils -Force

# 返回根目录
cd ../../
```

**最终目录结构预览：**
```
Cell/
├── backend/
│   ├── app/
│   │   ├── api/          # API 路由
│   │   ├── models/       # 数据模型
│   │   ├── services/     # 业务逻辑
│   │   └── utils/        # 工具函数
│   ├── configs/         # 配置文件
│   ├── weights/         # 模型权重
│   ├── temp/            # 临时文件
│   ├── logs/            # 日志
│   └── exports/         # 导出文件
├── frontend/            # 前端项目（稍后创建）
└── docs/               # 文档
```

#### ✅ 步骤 1.2.2：创建 .gitignore 文件

```bash
cd d:\Code\Python\cell

# 使用记事本创建 .gitignore
notepad .gitignore
```

**复制以下内容到 .gitignore：**
```gitignore
# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
venv/
ENV/
build/
*.egg-info/
.installed.cfg
*.egg

# Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
dist/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# 项目特定
backend/temp/*
!backend/temp/.gitkeep
backend/logs/*
!backend/logs/.gitkeep
backend/exports/*
!backend/exports/.gitkeep
backend/weights/*.pth
*.env
.env.local
.env.*
```

**保存并关闭记事本**

---

#### ✅ 步骤 1.2.3：创建 .gitkeep 占位文件

```bash
cd d:\Code\Python\cell

# 创建空的 .gitkeep 文件（PowerShell）
New-Item -ItemType File -Path "backend\temp\.gitkeep" -Force
New-Item -ItemType File -Path "backend\logs\.gitkeep" -Force
New-Item -ItemType File -Path "backend\exports\.gitkeep" -Force
New-Item -ItemType File -Path "backend\configs\.gitkeep" -Force
New-Item -ItemType File -Path "backend\weights\.gitkeep" -Force
```

**验证结果：**
```bash
# 检查文件是否创建成功
ls backend\temp\.gitkeep
ls backend\logs\.gitkeep
ls backend\exports\.gitkeep
```

---

#### ✅ 步骤 1.2.4：初始化 Git 仓库

```bash
cd d:\Code\Python\cell

# 初始化 Git 仓库
git init

# 查看所有文件状态
git status

# 添加所有文件到暂存区
git add .

# 第一次提交
git commit -m "Initial project structure - 项目初始化"

# 查看提交历史
git log --oneline
```

**预期输出：**
```
Initialized empty Git repository in d:/Code/Cell/.git/
[master (root-commit) abc1234] Initial project structure - 项目初始化
 X files changed, X insertions(+)
```

---

## 2. 第一阶段：基础架构搭建（Week 1-2）

### 2.1 后端基础（Day 2-4）

#### ✅ 步骤 2.1.1：安装 Python 依赖

```bash
cd d:\Code\Python\cell\backend

# 创建虚拟环境
python -m venv venv

# 验证虚拟环境已创建
ls venv\Scripts\activate.bat
```

**预期结果：** 看到 `venv` 文件夹

---

#### ✅ 步骤 2.1.2：激活虚拟环境

```bash
cd d:\Code\Python\cell\backend

# PowerShell 激活虚拟环境
.\venv\Scripts\Activate.ps1
```

**如果提示权限错误，执行：**
```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

**然后再次激活：**
```powershell
.\venv\Scripts\Activate.ps1
```

**验证激活成功：** 命令行前会出现 `(venv)` 前缀

---

#### ✅ 步骤 2.1.3：创建 requirements.txt 文件
notepad requirements.txt
```

**requirements.txt（第一版 - 基础依赖）：**
```txt
# Web 框架
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6

# 数据处理
pydantic==2.5.0
pydantic-settings==2.1.0
numpy==1.26.2

# 图像处理
pillow==10.1.0

# 工具库
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# 测试
pytest==7.4.3
httpx==0.25.2
```

```bash
# 安装依赖
pip install -r requirements.txt

# 验证安装
pip list
```

#### ✅ 步骤 2.1.2：创建 FastAPI 主应用

**文件：`backend/app/main.py`**

```python
from fastapi import FastAPI
from fastapi.middleware.cors import CORSMiddleware
import uvicorn
from pathlib import Path

# 创建 FastAPI 应用
app = FastAPI(
    title="细胞智能计数系统",
    version="1.0.0",
    description="细胞智能计数系统 API"
)

# 配置 CORS
app.add_middleware(
    CORSMiddleware,
    allow_origins=["http://localhost:3000", "http://localhost:5173"],
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)

# 健康检查接口
@app.get("/health")
async def health_check():
    return {
        "status": "ok",
        "version": "1.0.0"
    }

# 根路径
@app.get("/")
async def root():
    return {
        "message": "欢迎使用细胞智能计数系统 API",
        "docs": "/docs"
    }

if __name__ == "__main__":
    uvicorn.run("app.main:app", host="0.0.0.0", port=8000, reload=True)
```

#### ✅ 步骤 2.1.3：创建配置文件

**文件：`backend/app/config.py`**

```python
from pydantic_settings import BaseSettings
from pathlib import Path
import os

class Settings(BaseSettings):
    # 基础配置
    PROJECT_NAME: str = "细胞智能计数系统"
    VERSION: str = "1.0.0"
    
    # 服务器配置
    HOST: str = "0.0.0.0"
    PORT: int = 8000
    
    # 文件配置
    BASE_DIR: Path = Path(__file__).parent.parent
    UPLOAD_DIR: Path = BASE_DIR / "temp" / "uploads"
    EXPORT_DIR: Path = BASE_DIR / "temp" / "exports"
    MAX_FILE_SIZE: int = 20 * 1024 * 1024  # 20MB
    
    class Config:
        env_file = ".env"
        case_sensitive = True

settings = Settings()

# 确保目录存在
os.makedirs(settings.UPLOAD_DIR, exist_ok=True)
os.makedirs(settings.EXPORT_DIR, exist_ok=True)
```

**文件：`backend/.env`**

```bash
PROJECT_NAME=细胞智能计数系统
VERSION=1.0.0
PORT=8000
```

#### ✅ 步骤 2.1.4：测试后端启动

```bash
cd d:\Code\Python\cell\backend

# 激活虚拟环境
.\venv\Scripts\Activate.ps1

# 启动服务
python -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

**访问测试：**
- 浏览器打开：http://localhost:8000
- Swagger 文档：http://localhost:8000/docs
- 健康检查：http://localhost:8000/health

**预期结果：** 看到 JSON 响应表示成功

---

### 2.2 前端基础（Day 4-7）

#### ✅ 步骤 2.2.1：使用 Vite 创建 React 项目

```bash
cd d:\Code\Python\cell

# 创建 Vite + React + TypeScript 项目
npm create vite@latest frontend -- --template react-ts

cd frontend

# 安装基础依赖
npm install

# 安装额外依赖
npm install antd axios react-router-dom
npm install -D @types/node
```

#### ✅ 步骤 2.2.2：配置 Vite 代理

**文件：`frontend/vite.config.ts`**

```typescript
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'
import path from 'path'

export default defineConfig({
  plugins: [react()],
  resolve: {
    alias: {
      '@': path.resolve(__dirname, './src'),
    },
  },
  server: {
    port: 5173,
    proxy: {
      '/api': {
        target: 'http://localhost:8000',
        changeOrigin: true,
      },
    }
  }
})
```

#### ✅ 步骤 2.2.3：创建基础页面组件

**文件：`frontend/src/App.tsx`**

```tsx
import { BrowserRouter, Routes, Route } from 'react-router-dom'
import { ConfigProvider } from 'antd'
import zhCN from 'antd/locale/zh_CN'
import HomePage from '@/pages/HomePage'
import UploadPage from '@/pages/UploadPage'

function App() {
  return (
    <ConfigProvider locale={zhCN}>
      <BrowserRouter>
        <Routes>
          <Route path="/" element={<HomePage />} />
          <Route path="/upload" element={<UploadPage />} />
        </Routes>
      </BrowserRouter>
    </ConfigProvider>
  )
}

export default App
```

**文件：`frontend/src/pages/HomePage.tsx`**

```tsx
import { Button, Typography } from 'antd'
import { useNavigate } from 'react-router-dom'

const { Title } = Typography

const HomePage = () => {
  const navigate = useNavigate()

  return (
    <div style={{ padding: 40, textAlign: 'center' }}>
      <Title>细胞智能计数系统</Title>
      <Button 
        type="primary" 
        size="large"
        onClick={() => navigate('/upload')}
      >
        开始上传图像
      </Button>
    </div>
  )
}

export default HomePage
```

**文件：`frontend/src/pages/UploadPage.tsx`**

```tsx
import { Upload, message } from 'antd'
import { InboxOutlined } from '@ant-design/icons'
import axios from 'axios'

const { Dragger } = Upload

const UploadPage = () => {
  const uploadProps = {
    name: 'file',
    action: '/api/upload/single',
    multiple: false,
    maxCount: 1,
    accept: 'image/jpeg,image/png',
    beforeUpload: (file: File) => {
      // 验证文件大小
      if (file.size > 20 * 1024 * 1024) {
        message.error('文件大小不能超过 20MB')
        return false
      }
      return true
    },
    onChange: (info: any) => {
      if (info.file.status === 'done') {
        message.success(`${info.file.name} 上传成功`)
      } else if (info.file.status === 'error') {
        message.error(`${info.file.name} 上传失败`)
      }
    },
  }

  return (
    <div style={{ padding: 40 }}>
      <Dragger {...uploadProps}>
        <p className="ant-upload-drag-icon">
          <InboxOutlined />
        </p>
        <p className="ant-upload-text">点击或拖拽文件到此区域上传</p>
        <p className="ant-upload-hint">
          支持 JPEG/PNG 格式，单个文件不超过 20MB
        </p>
      </Dragger>
    </div>
  )
}

export default UploadPage
```

#### ✅ 步骤 2.2.4：启动前端服务

```bash
cd d:\Code\Python\cell\frontend

# 启动开发服务器
npm run dev
```

**访问测试：**
- http://localhost:5173

**预期结果：** 看到首页和上传按钮

---

### 2.3 联调测试（Day 8-10）

#### ✅ 步骤 2.3.1：同时启动前后端服务

**终端 1 - 后端：**
```bash
cd d:\Code\Python\cell\backend
.\venv\Scripts\Activate.ps1
python -m uvicorn app.main:app --reload
```

**终端 2 - 前端：**
```bash
cd d:\Code\Python\cell\frontend
npm run dev
```

#### ✅ 步骤 2.3.2：测试上传功能

1. 打开 http://localhost:5173
2. 点击"开始上传图像"
3. 选择一张测试图片
4. 检查 Network 面板和后端日志

**预期结果：** 图片成功上传并返回 file_id

---

## 3. 第二阶段：核心功能开发（Week 3-5）

### 3.1 预分析模块（Day 11-14）⭐重点

#### ✅ 步骤 3.1.1：安装深度学习依赖

```bash
cd d:\Code\Python\cell\backend

# 激活虚拟环境
.\venv\Scripts\Activate.ps1

# 安装 PyTorch（CPU 版本）
pip install torch==2.1.0 torchvision==0.16.0 --index-url https://download.pytorch.org/whl/cpu

# 或者 GPU 版本（需要 CUDA 11.7+）
# pip install torch==2.1.0 torchvision==0.16.0 --index-url https://download.pytorch.org/whl/cu117

# 安装 YOLOv8
pip install ultralytics==8.0.200
pip install opencv-python==4.8.1
```

#### ✅ 步骤 3.1.2：创建预分析服务

**文件：`backend/app/services/preanalyzer.py`**

```python
import numpy as np
from ultralytics import YOLO
import cv2
from pathlib import Path

class PreAnalyzer:
    """图像预分析器 - 提取细胞特征"""
    
    def __init__(self, model_path='yolov8n.pt'):
        """初始化预分析模型"""
        self.model = YOLO(model_path)
    
    def analyze(self, image_path: str) -> dict:
        """
        分析图像特征
        
        Args:
            image_path: 图像文件路径
            
        Returns:
            包含细胞数量、面积、密度等特征的字典
        """
        # 读取图像
        image = cv2.imread(image_path)
        h, w = image.shape[:2]
        
        # 执行检测
        results = self.model(image, verbose=False)
        boxes = results[0].boxes.xyxy.cpu().numpy()
        
        # 计算特征
        areas = [(x2-x1)*(y2-y1) for x1,y1,x2,y2 in boxes]
        avg_area = float(np.mean(areas)) if areas else 0.0
        std_area = float(np.std(areas)) if len(areas) > 1 else 0.0
        
        density = len(boxes) / (w * h)
        
        return {
            'cell_count': len(boxes),
            'avg_area': avg_area,
            'std_area': std_area,
            'density': float(density),
            'image_width': w,
            'image_height': h
        }

# 单例模式
_preanalyzer = None

def get_preanalyzer():
    """获取预分析器单例实例"""
    global _preanalyzer
    if _preanalyzer is None:
        _preanalyzer = PreAnalyzer()
    return _preanalyzer
```

#### ✅ 步骤 3.1.3：创建决策引擎（核心！）

**文件：`backend/app/services/model_selector.py`**

```python
import numpy as np
from enum import Enum
from dataclasses import dataclass
from typing import Tuple, List

class ModelChoice(Enum):
    """模型选择枚举"""
    DAS_DETR = "DAS-DETR"
    RDHS_YOLO = "RDHS-YOLO"

class ConfidenceLevel(Enum):
    """置信度级别"""
    HIGH = "high"
    MEDIUM = "medium"
    LOW = "low"

@dataclass
class DecisionResult:
    """决策结果数据结构"""
    model: ModelChoice
    confidence: ConfidenceLevel
    score: float
    reasons: List[str]
    features: dict

class AdvancedModelSelector:
    """高级模型选择器 - 基于多维度综合评分"""
    
    def __init__(self):
        # 权重配置
        self.weights = {
            'density_score': 0.30,
            'size_score': 0.25,
            'overlap_score': 0.20,
            'uniformity_score': 0.15,
            'count_score': 0.10
        }
        
        # 决策阈值
        self.thresholds = {
            'das_detr_strong': 0.70,
            'das_detr_weak': 0.55,
            'rdhs_yolo_weak': 0.40,
            'rdhs_yolo_strong': 0.25
        }
    
    def select(self, features: dict) -> DecisionResult:
        """
        根据图像特征选择最优模型
        
        Args:
            features: 包含 cell_count, avg_area, std_area, density 等
            
        Returns:
            DecisionResult: 决策结果
        """
        # Step 1: 归一化特征
        normalized = self._normalize_features(features)
        
        # Step 2: 计算综合得分
        score = self._calculate_score(normalized)
        
        # Step 3: 应用决策规则
        model, confidence, reasons = self._apply_rules(score, features)
        
        return DecisionResult(
            model=model,
            confidence=confidence,
            score=score,
            reasons=reasons,
            features=features
        )
    
    def _normalize_features(self, features: dict) -> dict:
        """特征归一化 - 映射到 [0,1] 区间"""
        normalized = {}
        
        # 密度评分
        normalized['density_score'] = 1 / (1 + np.exp(-1000 * (features['density'] - 0.0005)))
        
        # 面积评分（小细胞适合 DAS-DETR）
        normalized['size_score'] = 1 / (1 + np.exp(0.01 * (features['avg_area'] - 200)))
        
        # 重叠评分
        normalized['overlap_score'] = min(features.get('max_iou', 0), 1.0)
        
        # 均匀性评分
        size_cv = features.get('std_area', 0) / (features['avg_area'] + 1e-6)
        normalized['uniformity_score'] = 1 / (1 + size_cv)
        
        # 数量评分
        normalized['count_score'] = 1 / (1 + np.exp(-0.1 * (features['cell_count'] - 30)))
        
        return normalized
    
    def _calculate_score(self, normalized: dict) -> float:
        """计算加权综合得分"""
        score = sum(
            normalized[f'{key}_score'] * weight 
            for key, weight in self.weights.items()
        )
        return min(max(score, 0.0), 1.0)
    
    def _apply_rules(self, score: float, features: dict) -> Tuple[ModelChoice, ConfidenceLevel, List[str]]:
        """应用决策规则"""
        if score >= self.thresholds['das_detr_strong']:
            return (
                ModelChoice.DAS_DETR,
                ConfidenceLevel.HIGH,
                [
                    f"细胞密度高 (ρ={features['density']:.6f})",
                    f"平均面积小 ({features['avg_area']:.1f} pixels²)",
                    "DAS-DETR 在密集小细胞场景下表现更优"
                ]
            )
        elif score >= self.thresholds['das_detr_weak']:
            return (
                ModelChoice.DAS_DETR,
                ConfidenceLevel.MEDIUM,
                ["细胞密度较高，DAS-DETR 更适合"]
            )
        elif score >= self.thresholds['rdhs_yolo_weak']:
            return (
                ModelChoice.RDHS_YOLO,
                ConfidenceLevel.LOW,
                ["场景特征不显著，优先选择 RDHS-YOLO"]
            )
        else:
            return (
                ModelChoice.RDHS_YOLO,
                ConfidenceLevel.HIGH,
                [
                    f"细胞稀疏 (ρ={features['density']:.6f})",
                    "RDHS-YOLO 能提供更精确的轮廓"
                ]
            )

# 单例
_selector = None

def get_model_selector():
    """获取模型选择器单例"""
    global _selector
    if _selector is None:
        _selector = AdvancedModelSelector()
    return _selector
```

#### ✅ 步骤 3.1.4：创建预分析 API

**文件：`backend/app/api/inference.py`**

```python
from fastapi import APIRouter, HTTPException
from pydantic import BaseModel
from typing import Optional
import uuid

from app.services.preanalyzer import get_preanalyzer
from app.services.model_selector import get_model_selector, ModelChoice

router = APIRouter()

class PreanalysisRequest(BaseModel):
    file_id: str

class PreanalysisResponse(BaseModel):
    features: dict
    selected_model: str
    confidence: str
    score: float
    reasons: list

@router.post("/preanalysis", response_model=PreanalysisResponse)
async def preanalysis(request: PreanalysisRequest):
    """预分析并选择模型"""
    try:
        # TODO: 从文件系统加载图像
        file_path = f"temp/uploads/{request.file_id}"
        
        # 执行预分析
        preanalyzer = get_preanalyzer()
        features = preanalyzer.analyze(file_path)
        
        # 模型选择
        selector = get_model_selector()
        decision = selector.select(features)
        
        return PreanalysisResponse(
            features=features,
            selected_model=decision.model.value,
            confidence=decision.confidence.value,
            score=decision.score,
            reasons=decision.reasons
        )
    except Exception as e:
        raise HTTPException(500, str(e))
```

---

### 3.2 模型推理模块（Day 15-25）

#### ✅ 步骤 3.2.1：准备模型权重文件

```bash
cd d:\Code\Python\cell\backend\weights

# 下载或复制你的模型权重文件
# 1. RDHS-YOLO 权重：rdhs_yolo_best.pth
# 2. DAS-DETR 权重：das_detr_best.pth

# 如果没有真实权重，先用占位符
echo "Placeholder" > rdhs_yolo_best.pth
echo "Placeholder" > das_detr_best.pth
```

#### ✅ 步骤 3.2.2：创建模型管理器

**文件：`backend/app/services/model_manager.py`**

```python
import torch
from pathlib import Path
from app.config import settings

class ModelManager:
    """模型管理器 - 单例模式"""
    
    _instance = None
    
    def __new__(cls):
        if cls._instance is None:
            cls._instance = super().__new__(cls)
            cls._instance.rdhs_yolo = None
            cls._instance.das_detr = None
            cls._instance._initialized = False
        return cls._instance
    
    def initialize(self):
        """初始化模型"""
        if self._initialized:
            return
        
        device = torch.device('cuda' if torch.cuda.is_available() else 'cpu')
        
        # 加载 RDHS-YOLO（TODO: 替换为真实加载代码）
        try:
            weight_path = Path(settings.BASE_DIR) / "weights" / "rdhs_yolo_best.pth"
            if weight_path.exists():
                self.rdhs_yolo = torch.load(weight_path, map_location=device)
                self.rdhs_yolo.eval()
                print("✓ RDHS-YOLO loaded")
            else:
                print("⚠ RDHS-YOLO weights not found")
        except Exception as e:
            print(f"✗ Failed to load RDHS-YOLO: {e}")
        
        # 加载 DAS-DETR（TODO: 替换为真实加载代码）
        try:
            weight_path = Path(settings.BASE_DIR) / "weights" / "das_detr_best.pth"
            if weight_path.exists():
                self.das_detr = torch.load(weight_path, map_location=device)
                self.das_detr.eval()
                print("✓ DAS-DETR loaded")
            else:
                print("⚠ DAS-DETR weights not found")
        except Exception as e:
            print(f"✗ Failed to load DAS-DETR: {e}")
        
        self._initialized = True
    
    def get_model(self, model_name: str):
        """获取指定模型"""
        if model_name == 'RDHS-YOLO':
            return self.rdhs_yolo
        elif model_name == 'DAS-DETR':
            return self.das_detr
        else:
            raise ValueError(f"Unknown model: {model_name}")

# 全局实例
model_manager = ModelManager()
```

---

### 3.3 结果可视化（Day 26-35）

#### ✅ 步骤 3.3.1：安装 Canvas 库

```bash
cd d:\Code\Python\cell\frontend

npm install react-konva konva
npm install -D @types/konva
```

#### ✅ 步骤 3.3.2：创建图像查看器组件

**文件：`frontend/src/components/ImageViewer.tsx`**

```tsx
import { Stage, Layer, Image as KonvaImage, Group, Rect, Text } from 'react-konva'
import { useState, useRef, useEffect } from 'react'

interface ImageViewerProps {
  imageUrl: string
  annotations?: any[]
  showAnnotations?: boolean
}

const ImageViewer: React.FC<ImageViewerProps> = ({
  imageUrl,
  annotations = [],
  showAnnotations = true
}) => {
  const [imageSize, setImageSize] = useState({ width: 800, height: 600 })
  const imageRef = useRef<HTMLImageElement>(null)

  useEffect(() => {
    const img = new window.Image()
    img.crossOrigin = 'anonymous'
    img.src = imageUrl
    img.onload = () => {
      setImageSize({ width: img.width, height: img.height })
      imageRef.current = img
    }
  }, [imageUrl])

  return (
    <Stage width={imageSize.width} height={imageSize.height}>
      <Layer>
        {imageRef.current && (
          <KonvaImage
            image={imageRef.current}
            width={imageSize.width}
            height={imageSize.height}
          />
        )}
      </Layer>
      
      {showAnnotations && (
        <Layer>
          {annotations.map((anno, idx) => (
            <Group key={idx}>
              <Rect
                x={anno.bbox[0]}
                y={anno.bbox[1]}
                width={anno.bbox[2] - anno.bbox[0]}
                height={anno.bbox[3] - anno.bbox[1]}
                stroke={anno.color}
                strokeWidth={2}
                fill="transparent"
              />
              <Text
                x={anno.bbox[0]}
                y={anno.bbox[1] - 20}
                text={`${anno.label} ${(anno.confidence * 100).toFixed(0)}%`}
                fontSize={12}
                fill={anno.color}
              />
            </Group>
          ))}
        </Layer>
      )}
    </Stage>
  )
}

export default ImageViewer
```

---

## 4. 第三阶段：功能完善（Week 6-7）

### 4.1 导出模块（Day 36-42）

#### ✅ 步骤 4.1.1：实现图像导出

**文件：`backend/app/api/export.py`**

```python
from fastapi import APIRouter, HTTPException
from fastapi.responses import FileResponse
import cv2
import numpy as np
from pathlib import Path

from app.config import settings

router = APIRouter()

@router.post("/image/{file_id}")
async def export_image(file_id: str, format: str = "png"):
    """导出带标注的结果图"""
    # TODO: 从缓存获取结果
    # TODO: 使用 OpenCV 绘制标注
    # TODO: 保存并返回下载链接
    
    output_path = settings.EXPORT_DIR / f"{file_id}_result.{format}"
    
    return {
        "download_url": f"/downloads/{file_id}_result.{format}"
    }
```

---

### 4.2 历史记录模块（Day 43-49）

#### ✅ 步骤 4.2.1：安装 Redis

```bash
# Windows 用户使用 Docker 安装
docker run -d -p 6379:6379 --name redis redis:7-alpine

# 或使用 WSL2 安装
```

---

## 5. 第四阶段：测试与部署（Week 8）

### 5.1 单元测试（Day 50-52）

#### ✅ 步骤 5.1.1：编写决策引擎测试

**文件：`backend/tests/test_model_selector.py`**

```python
import pytest
from app.services.model_selector import AdvancedModelSelector, ModelChoice

def test_dense_cells_scenario():
    """测试密集小细胞场景"""
    selector = AdvancedModelSelector()
    features = {
        'cell_count': 85,
        'avg_area': 65.3,
        'std_area': 28.4,
        'density': 0.00163,
        'max_iou': 0.78
    }
    
    result = selector.select(features)
    
    assert result.model == ModelChoice.DAS_DETR
    assert result.confidence == ConfidenceLevel.HIGH
    assert result.score > 0.7

def test_sparse_cells_scenario():
    """测试稀疏大细胞场景"""
    selector = AdvancedModelSelector()
    features = {
        'cell_count': 12,
        'avg_area': 485.7,
        'density': 0.00009,
        'max_iou': 0.15
    }
    
    result = selector.select(features)
    
    assert result.model == ModelChoice.RDHS_YOLO
    assert result.confidence == ConfidenceLevel.HIGH
```

---

## 6. 每日检查清单

### Day 1-2: 环境准备
- [ ] Python 安装成功
- [ ] Node.js 安装成功
- [ ] Git 配置完成
- [ ] 项目目录创建完成
- [ ] Git 仓库初始化

### Day 3-4: 后端基础
- [ ] requirements.txt 创建
- [ ] 虚拟环境激活
- [ ] FastAPI 应用启动
- [ ] Swagger 文档可访问
- [ ] 配置文件生效

### Day 5-7: 前端基础
- [ ] Vite 项目创建
- [ ] React 组件正常渲染
- [ ] Ant Design 样式正常
- [ ] 路由跳转正常
- [ ] 前后端联调成功

### Day 8-10: 上传功能
- [ ] 文件上传成功
- [ ] 文件验证生效
- [ ] 错误提示正常
- [ ] 后端接收成功

### Day 11-14: 预分析模块
- [ ] YOLOv8 安装成功
- [ ] 预分析服务运行
- [ ] 特征提取正确
- [ ] 决策引擎测试通过

---

## 7. 常见问题与解决方案

### ❌ 问题 1：虚拟环境无法激活

**PowerShell:**
```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
.\venv\Scripts\Activate.ps1
```

### ❌ 问题 2：PyTorch 安装失败

**解决方案：**
```bash
# 使用国内镜像
pip install torch==2.1.0 -i https://pypi.tuna.tsinghua.edu.cn/simple
```

### ❌ 问题 3：CORS 错误

**检查：**
```python
# backend/app/main.py
app.add_middleware(
    CORSMiddleware,
    allow_origins=["http://localhost:5173"],  # 确保端口正确
    # ...
)
```

### ❌ 问题 4：导入错误 ModuleNotFoundError

**解决方案：**
```bash
# 确保在项目根目录运行
cd d:\Code\Python\cell\backend
python -m uvicorn app.main:app --reload
```

---

## 🎯 下一步行动

完成本方案后，你将拥有：
1. ✅ 完整的前后端分离架构
2. ✅ 决策引擎核心功能
3. ✅ 基础的图像上传和展示
4. ✅ 可扩展的项目结构

**后续工作：**
- 集成真实的 RDHS-YOLO 和 DAS-DETR 模型
- 完善结果可视化
- 添加批量处理功能
- 性能优化和测试

**文档版本：** v1.0  
**最后更新：** 2026-03-07  
**作者：** 卢伶利
